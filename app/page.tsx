import { Section } from "@/components/layout/Section";
import { SectionIntro } from "@/components/shared/SectionIntro";

// Placeholder home — full assembly lands in Tier 4.
export default function HomePage() {
  return (
    <Section>
      <SectionIntro headline="Portfolio in progress" align="center">
        <p>Layout shell is live. Pages are being assembled tier by tier.</p>
      </SectionIntro>
    </Section>
  );
}
